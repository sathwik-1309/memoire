development:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL_CABLE") { "redis://localhost:6379/1" } %>

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL_CABLE") { "redis://localhost:6379/1" } %>
  channel_prefix: audit_production
